################## TODO ##################

* FIX: choose a font with I and L different!!
* ADD: select with mouse moving!!
* ADD: detect (avail port to share)
* ADD: electron setProgressBar
* FIX: gh-actions is always overwriting latest release!!
* REFACTOR: use web-share!!!
* ADD: playback rate
* ADD: cut and visualise audio

################## NOTES ##################

1. `const { getState: getPage } = usePage;
		// and then:
		getPage().page
	 ` is not reactive!

2. `const { isLoadingMedias } = usePlaylists();`
		will rerender not only with `isLoadingMedias`,
		you have to use a select function.

3. If ever needed, see last.fm for lyrics, or lrc

4. If ever needed, make a data structure that has an indexed map.
