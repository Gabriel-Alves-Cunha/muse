import { styled } from "@styles/global";

export const FileSize = styled("p", {
	dflex: "center",
	h: "100%",

	c: "$gray-text",
	ff: "$primary",
	ls: "0.03rem",
	fs: "0.9rem",
	ta: "center",
	fw: 500,
});
