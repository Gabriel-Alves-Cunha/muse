import { styled } from "@styles/global";

export const Header = styled("header", {
	position: "relative",
	display: "flex", // row,
	justifyContent: "space-between",
	alignItems: "center",

	padding: "0 5%",
	height: 60,
	mt: 10,
});
