import { styled } from "@styles/global";

export const Header = styled("header", {
	position: "relative",
	display: "flex", // row,
	justifyContent: "space-between",
	alignItems: "center",
	h: 60,

	gap: "1rem",
	mb: "5%",
	mt: 10,
});
