import { styled } from "@styles/global";

export const Header = styled("header", {
	position: "relative",
	display: "flex", // row,
	justifyContent: "space-between",
	alignItems: "center",
	h: 60,

	p: "0 5%",
	mt: 10,
});
